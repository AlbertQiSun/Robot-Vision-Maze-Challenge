name: game
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10.12
  - pip=23.2.1
  - pybullet=3.24
  - pip:
    - --extra-index-url https://test.pypi.org/simple/
    - numpy==1.25.2
    - opencv_python==4.8.0.76
    - cython~=3.0.2
    - pygame~=2.5.1
    - matplotlib>=3.5.3
    - cryptography==41.0.3
    - gdown~=4.7.1
    - scikit-learn~=1.3.0
    - vis-nav-game
    - natsort==8.4.0
    - networkx==3.4.2
    # --- Deep learning (auto-detects CUDA if available) ---
    - torch>=2.1.0
    - torchvision>=0.16.0
    - tqdm>=4.65.0
    # --- FAISS for fast nearest-neighbor search ---
    - faiss-cpu>=1.7.4
    # --- Kornia for SuperPoint + LightGlue ---
    - kornia>=0.7.0
    - kornia-rs>=0.1.0
